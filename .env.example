# Porta da API FastAPI (app/main.py)
PORT=3000

# Pasta onde as imagens de carta geradas sao salvas
OUTPUT_DIR=output

# false abre a janela do Chrome durante a automacao (debug visual);
# default (true) roda sem janela
HEADLESS=true
